# This is where i keep my .gitignore files.
# node
node_modules/
dist/
.vite/

# go
/backend/devboard-backend

# env — keep .env local; the committed template is .env.example
.env

# misc
.DS_Store
*.local

# claude's personal scratch notes (local only)
CLAUDE.md
